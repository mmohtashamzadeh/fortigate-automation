module "fw15" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw15 }

  interfaces = local.firewalls["FW15"].interfaces
  addresses  = local.firewalls["FW15"].addresses
  services   = local.firewalls["FW15"].services
  policies   = local.firewalls["FW15"].policies
}

module "fw16" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw16 }

  interfaces = local.firewalls["FW16"].interfaces
  addresses  = local.firewalls["FW16"].addresses
  services   = local.firewalls["FW16"].services
  policies   = local.firewalls["FW16"].policies
}

module "fw17" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw17 }

  interfaces = local.firewalls["FW17"].interfaces
  addresses  = local.firewalls["FW17"].addresses
  services   = local.firewalls["FW17"].services
  policies   = local.firewalls["FW17"].policies
}

module "fw18" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw18 }

  interfaces = local.firewalls["FW18"].interfaces
  addresses  = local.firewalls["FW18"].addresses
  services   = local.firewalls["FW18"].services
  policies   = local.firewalls["FW18"].policies
}

module "fw19" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw19 }

  interfaces = local.firewalls["FW19"].interfaces
  addresses  = local.firewalls["FW19"].addresses
  services   = local.firewalls["FW19"].services
  policies   = local.firewalls["FW19"].policies
}

module "fw20" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw20 }

  interfaces = local.firewalls["FW20"].interfaces
  addresses  = local.firewalls["FW20"].addresses
  services   = local.firewalls["FW20"].services
  policies   = local.firewalls["FW20"].policies
}

module "fw21" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw21 }

  interfaces = local.firewalls["FW21"].interfaces
  addresses  = local.firewalls["FW21"].addresses
  services   = local.firewalls["FW21"].services
  policies   = local.firewalls["FW21"].policies
}

module "fw22" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw22 }

  interfaces = local.firewalls["FW22"].interfaces
  addresses  = local.firewalls["FW22"].addresses
  services   = local.firewalls["FW22"].services
  policies   = local.firewalls["FW22"].policies
}

module "fw23" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw23 }

  interfaces = local.firewalls["FW23"].interfaces
  addresses  = local.firewalls["FW23"].addresses
  services   = local.firewalls["FW23"].services
  policies   = local.firewalls["FW23"].policies
}

module "fw24" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw24 }

  interfaces = local.firewalls["FW24"].interfaces
  addresses  = local.firewalls["FW24"].addresses
  services   = local.firewalls["FW24"].services
  policies   = local.firewalls["FW24"].policies
}

module "fw25" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw25 }

  interfaces = local.firewalls["FW25"].interfaces
  addresses  = local.firewalls["FW25"].addresses
  services   = local.firewalls["FW25"].services
  policies   = local.firewalls["FW25"].policies
}

module "fw26" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw26 }

  interfaces = local.firewalls["FW26"].interfaces
  addresses  = local.firewalls["FW26"].addresses
  services   = local.firewalls["FW26"].services
  policies   = local.firewalls["FW26"].policies
}

module "fw27" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw27 }

  interfaces = local.firewalls["FW27"].interfaces
  addresses  = local.firewalls["FW27"].addresses
  services   = local.firewalls["FW27"].services
  policies   = local.firewalls["FW27"].policies
}

module "fw28" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw28 }

  interfaces = local.firewalls["FW28"].interfaces
  addresses  = local.firewalls["FW28"].addresses
  services   = local.firewalls["FW28"].services
  policies   = local.firewalls["FW28"].policies
}

module "fw29" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw29 }

  interfaces = local.firewalls["FW29"].interfaces
  addresses  = local.firewalls["FW29"].addresses
  services   = local.firewalls["FW29"].services
  policies   = local.firewalls["FW29"].policies
}

module "fw30" {
  source    = "./modules/fortigate_device"
  providers = { fortios = fortios.fw30 }

  interfaces = local.firewalls["FW30"].interfaces
  addresses  = local.firewalls["FW30"].addresses
  services   = local.firewalls["FW30"].services
  policies   = local.firewalls["FW30"].policies
}

